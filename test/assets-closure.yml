javascript_compressor: closure
compressor_options:
  compilation_level: ADVANCED_OPTIMIZATIONS
embed_images: on

javascripts:
  test:
    - fixtures/src/*.js

stylesheets:
  test:
    - fixtures/src/*.css

templates:
  test:
    - fixtures/src/*.jst
