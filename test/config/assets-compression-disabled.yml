javascript_compressor: yui
compress_assets: false
embed_images: on

javascripts:
  test:
    - fixtures/src/*.js

stylesheets:
  test:
    - fixtures/src/*.css

templates:
  test:
    - fixtures/src/*.jst
